#include<iostream>
using namespace std;
typedef struct node 
{
	struct node* left;
	struct node* right;
	int data;
}tnode;

tnode* getNode(int data)
{
	tnode* pNode = new tnode;
	pNode->data = data;
	pNode->left = NULL;
	pNode->right = NULL;
	return pNode;
}